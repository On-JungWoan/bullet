from django.shortcuts import render

#유저는 데이터를 조회 이외에는 하지 않는다.
#프론트에서 id를 통해 post를 검색
def findById():
    
    return

#프론트엔드에서 user의 id를 통해 그 유저가 등록한 뉴스를 검색할 수 있음
def findByUserId():
    return

# Create your views here.
